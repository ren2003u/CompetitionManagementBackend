spring:
  datasource:
    url: jdbc:mysql://localhost:3306/competition_management?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: 111111
    driver-class-name: com.mysql.cj.jdbc.Driver
#  jpa:
#    show-sql: true
#    hibernate:
#      ddl-auto: update
#    properties:
#      hibernate:
#        format_sql: true

mybatis:
  mapper-locations: classpath:mapper/**Mapper.xml
  configuration: # 配置打印 MyBatis 执行的 SQL
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


server:
  port: 8080

logging:
  level:
    org:
      springframework: INFO
      hibernate: ERROR

#vue:
#  client:
#    resources: /path/to/vue/client/dist
